<h1>FizzBuzz Hundred Home Page</h1>
<%= link_to 'Go to Populate with Pagination Page', url_for(action: 'populate') %>
<BR />&nbsp;
<table>
  <% @rows.each do |row| %>
    <tr>
      <td><%= row[:key] %></td>
      <td><%= row[:value] %></td>
    </tr>
  <% end %>
</table>
